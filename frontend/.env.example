# Copy to .env (git-ignored) and adjust as needed.

# Display name shown in the browser tab title, PWA manifest/install
# prompt, and in-app header/login screen. Also written into
# public/manifest.json at dev-server-start/build time -- see the
# writeManifest plugin in vite.config.js.
VITE_APP_NAME=Role Pace
